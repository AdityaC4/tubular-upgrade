// Order-sensitive Fibonacci tail recursion

function fib_tail(int n, int a, int b) : int {
  if (n == 0) return a;
  return fib_tail(n - 1, b, a + b);
}

function main() : int {
  return fib_tail(6, 0, 1);
}
