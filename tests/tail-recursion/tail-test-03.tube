// Tail-recursive Fibonacci with accumulators: fib(n,a,b)
// fib(0,a,b) = a; fib(1,a,b) = b; fib(n,a,b) = fib(n-1, b, a+b)
// main returns fib(30, 0, 1) = 832040

function fib(int n, int a, int b) : int {
  if (n == 0) return a;
  if (n == 1) return b;
  return fib(n - 1, b, a + b);
}

function main() : int {
  return fib(30, 0, 1);
}

