// Simple non-cyclic sum test that definitely works
// sum_down(n, acc) = acc if n<=0, else sum_down(n-1, acc+n)
// main returns sum_down(1000, 0) = 500500

function sum_down(int n, int acc) : int {
  if (n <= 0) return acc;
  return sum_down(n - 1, acc + n);
}

function main() : int {
  return sum_down(1000, 0);
}

