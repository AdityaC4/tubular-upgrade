// Tail-recursive factorial with accumulator; main returns fact(5) * 1 + 115 = 120

function fact(int n, int acc) : int {
  if (n == 0) return acc;
  return fact(n - 1, n * acc);
}

function main() : int {
  return fact(5, 1) + 115;
}

