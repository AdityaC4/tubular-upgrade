// Arithmetic loop benchmark
function SumTo(int n) : int {
  int total = 0;
  int i = 0;
  while (i < n) {
    int triple = i * 3;
    int half = triple / 2;
    total = total + triple - half;
    i = i + 1;
  }
  return total;
}

function main() : int {
  return SumTo(2000);
}
